/** Shared layout for stacked action cards on the /app painel sidebar (equal height, not square). */
export const PAINEL_ACTION_CARD_SHELL =
  "flex flex-col items-center justify-center text-center w-full h-48 p-4";
