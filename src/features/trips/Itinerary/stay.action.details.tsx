import { TripStaySimplified } from "@/core/types";

interface Props {
    action: TripStaySimplified;
    tripId: string;
    onCloseModal: () => void
}
export const StayActionDetails = ({}: Props) => {
    return <div>Details</div>
}